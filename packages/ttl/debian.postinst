#!/bin/sh
set -e

case "$1" in
    configure)
        if command -v setcap >/dev/null 2>&1; then
            setcap cap_net_raw+ep /usr/bin/ttl || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
